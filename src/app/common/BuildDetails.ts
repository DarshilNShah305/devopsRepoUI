export class BuildDetails {
    public gitUrl: string;
    public os: string;
    public buildTool: string;
    public language: string;
    public langVersion: string;
    public cloud: number;
    public dockerize: boolean;

    constructor() {}
}
